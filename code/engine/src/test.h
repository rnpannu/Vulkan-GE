#pragma once

#include "defines.h"

KAPI void  print_int(i32 i); // "KAPI declspec(dllexport) Want to export this function for use in other libraries or executables"

// Debug information is in engine.pdb and testbed.pdb respectively